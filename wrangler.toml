name = "schemabio-backend"
main = "src/index.py"
compatibility_date = "2024-04-05"
compatibility_flags = ["python_workers"]

# 绑定环境变量 (本地开发占位符，实际值在 Dashboard 或 Secrets 配置)
[vars]
# GEMINI_API_KEY = "secret"
# FIREBASE_CREDENTIALS = "secret"

# 允许前端访问
[observability]
enabled = true
